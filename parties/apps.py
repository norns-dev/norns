"""App config for parties app"""

from django.apps import AppConfig


class PartiesConfig(AppConfig):
    """App config for parties app"""

    default_auto_field = "django.db.models.BigAutoField"
    name = "parties"
